# 2026-07-01

## deepseek-pc → Tauri 迁移技术报告
- 研读了 `D:\code\deepseek-pc` (Go + Wails3) 完整源码，包括 6 个 internal 包、前端（纯 JS + Vite）、注入脚本
- 分析了 Tauri 2 API（基于 `D:\wiki\tauri\zh-cn` 文档）：global-shortcut 插件、system-tray、autostart 插件、多窗口等
- 生成了 `D:\code\quick-web-v2\docs\deepseek-pc-tauri-migration-report.md`
- 核心发现：前端代码可以几乎全部复用（纯 JS 无框架），后端 6 个模块映射为 3-4 个 Rust 模块 + 3 个 Tauri 插件
- 关键差异：Tauri 多窗口用 `WebviewWindowBuilder`，热键用 `global-shortcut` 插件，脚本注入用 `eval()`

## deepseek-pc → Tauri 迁移实施
- 完整实现了所有功能，`pnpm tauri build` 通过
- Rust 后端 3 个模块：lib.rs（主入口+热键+托盘+窗口管理+命令）、settings.rs（设置模型+CRUD+目录）、logger.rs（文件日志+小时轮转+72h保留）
- 前端：index.html + styles.css + main.js 从 deepseek-pc 完整复制并适配 Tauri invoke API
- Tauri 插件：tauri-plugin-global-shortcut（全局热键）、tauri-plugin-autostart（开机自启）、tray-icon（系统托盘）、tauri-plugin-opener
- 注入脚本（batch-delete.js/focus-textarea.js）通过 include_str! 编译时嵌入，首次启动释放到 appdata
- 已知 ponytail: Tauri 2 单实例 API 尚未实现（不同版本间 API 变化大）
- 输出：quick-web-v2.exe (release) + MSI + NSIS installer
