# 2026-06-26

- deepseek-pc 新增"启动时最小化(隐藏到托盘)"开关配置。后端 Data 结构体加 StartMinimized bool，main.go 条件调用 wm.Show()，前端 Settings 页面新增 toggle section，bindings 同步更新。功能零破坏。

- 完成架构审查 6 项修复：C2 hotkey goroutine 泄漏(context.WithCancel)、C3 删除 5 处死代码(injector 包/ToggleHandler/OnAfterCreate/RegisterAndSwap/RegisterEvent)、C4 合并 createWindow 消除重复、C1 提取 internal/coordinator.Service(main.go 210→120行)、C5 脚本注入改用重试机制替代 time.Sleep、C6 settings.Save 添加 RLock 语义注释。

- deepseek-pc 多站点架构重构：(1) 新增 SiteEntry 数据模型（ID/URL/Name/Hotkey/ScriptDir），Data.Entries 替代旧单 Hotkey；(2) hotkey.Service 改为 map 存储支持多热键同时注册（Register(keystr)/Unregister(keystr)）；(3) webview.Manager 新增 siteWindows/siteTimers map，支持 SiteShow/SiteHide/SiteIsVisible/DestroySiteWindow；(4) 新建 internal/scripts/loader.go，LoadFromDir 从文件系统读 .js，ReleaseEmbedded 首次释放 go:embed 脚本到 appdata；(5) main.go 移除旧 setupHotkey，改为遍历 entries 调用 registerEntryHotkey，OnSave 回调处理增删改热键 + 销毁已删窗口；(6) 前端 index.html 重写为站点列表 + 编辑弹窗（热键选择器/脚本目录/打开资源管理器），main.js 实现 CRUD 逻辑；(7) settings.Service 新增 OnSave 回调、ScriptDir/OpenScriptDir/MarkScriptsReleased 方法，initDefaults 首次创建默认 DeepSeek 条目并迁移旧 hotkey。编译通过，前端 bindings 需 wails build 重新生成。
